# classe utilisée pour mettre à jour les données dans Airtable
from pyairtable import Api

class update_AT : 
    def ___init(self, my_base_id: str, my_api_key: str) :
        self.my_base_id = my_base_id
        self.my_api_key = my_api_key
        
   # Initialiser la table des Resultats après les poules
   # Initialiser la table des Poules après les poules 
